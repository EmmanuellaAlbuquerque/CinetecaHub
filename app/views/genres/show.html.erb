<h1>Detalhes do Gênero: <%= @genre.name %></h1>

<% if @movies.length == 0 %>
  <h3>Não existem filmes associados ao gênero!</h3>
<% end %>

<% @movies.each do | movie | %>
  <%= movie.title %>
  <%= link_to 'Detalhes', movie_path(movie) %>
<% end %>
